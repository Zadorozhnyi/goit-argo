apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    purpose: production
---
# The inference-service Deployment/Service land here once the FastAPI image
# is built (Block B3). Production runs whichever model version is currently
# in MLflow Registry stage "Production" - promoted from staging on purpose,
# never trained directly against this namespace.
