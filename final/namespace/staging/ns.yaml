apiVersion: v1
kind: Namespace
metadata:
  name: staging
  labels:
    purpose: staging
---
# The inference-service Deployment/Service land here once the FastAPI image
# is built (Block B3). Staging always runs the model version currently in
# MLflow Registry stage "Staging" - production traffic never touches it.
